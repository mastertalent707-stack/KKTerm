# Pending localization: settings.sshOldProtocolsHint

- **Key**: `settings.sshOldProtocolsHint`
- **English source**: See `src/i18n/locales/en.json`.
- **Context**: SSH Settings guidance for the Legacy Protocols switch. It warns that the switch enables every supported legacy SSH compatibility algorithm at once and should be used only for trusted older servers. Keep SSH recognizable.
- **Domain notes**: Use KKTerm domain terms: Connection is durable, Session is live runtime. zh-TW must use Taiwan computing terminology.
