# settings.urlProxyHost

- **English value**: `Proxy host`
- **Namespace**: `settings`
- **File/component**: `src/modules/settings/UrlSettings.tsx`
- **UI role**: `label`
- **User flow**: The user enters the proxy server hostname or IP address.
- **Tone**: concise/neutral
- **Placeholders**: none
- **Context/meaning**: Host means the proxy server address, not the URL destination.
- **Domain notes**: URL Connection is a durable KKTerm Connection; URL, HTTP, HTTPS, SOCKS5, Proxy, TCP, and macOS are technical terms.

