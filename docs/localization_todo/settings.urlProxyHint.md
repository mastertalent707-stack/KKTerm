# settings.urlProxyHint

- **English value**: `Default network route for new URL Connection tabs. HTTP proxies tunnel both HTTP and HTTPS destinations.`
- **Namespace**: `settings`
- **File/component**: `src/modules/settings/UrlSettings.tsx`
- **UI role**: `label`
- **User flow**: The user sees this guidance when configuring the global URL proxy.
- **Tone**: concise/neutral
- **Placeholders**: none
- **Context/meaning**: HTTP proxy here means HTTP CONNECT and covers HTTPS destinations.
- **Domain notes**: URL Connection is a durable KKTerm Connection; URL, HTTP, HTTPS, SOCKS5, Proxy, TCP, and macOS are technical terms.

