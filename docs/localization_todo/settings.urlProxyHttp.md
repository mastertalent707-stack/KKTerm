# settings.urlProxyHttp

- **English value**: `HTTP / HTTPS`
- **Namespace**: `settings`
- **File/component**: `src/modules/settings/UrlSettings.tsx`
- **UI role**: `label`
- **User flow**: The user selects this for an HTTP CONNECT proxy.
- **Tone**: concise/neutral
- **Placeholders**: none
- **Context/meaning**: This one HTTP proxy mode supports both HTTP and HTTPS destinations.
- **Domain notes**: URL Connection is a durable KKTerm Connection; URL, HTTP, HTTPS, SOCKS5, Proxy, TCP, and macOS are technical terms.

