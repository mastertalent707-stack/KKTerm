# settings.urlProxyDirect

- **English value**: `Direct (no proxy)`
- **Namespace**: `settings`
- **File/component**: `src/modules/settings/UrlSettings.tsx`
- **UI role**: `label`
- **User flow**: The user selects this to bypass proxy routing.
- **Tone**: concise/neutral
- **Placeholders**: none
- **Context/meaning**: Direct means connect without a proxy.
- **Domain notes**: URL Connection is a durable KKTerm Connection; URL, HTTP, HTTPS, SOCKS5, Proxy, TCP, and macOS are technical terms.

